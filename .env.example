# ACE runs fully local — no external platform required.
# Store your Claude API key in the app's Settings page.

# TERMINAL GATE — Master Key hash (required)
# Generate with: echo -n "your-password" | shasum -a 256 | cut -d' ' -f1
# Then paste the hash here. The app stays locked without it.
VITE_APP_HASH=
